services:
  mysql:
    image: "mysql:8.4"
    environment:
      - "MYSQL_DATABASE=nabom"
      - "MYSQL_USER=nabom"
      - "MYSQL_PASSWORD=secret"
      - "MYSQL_ROOT_PASSWORD=verysecret"
    ports:
      - "3306"

  redis:
    image: "redis:7-alpine"
    ports:
      - "6379"
