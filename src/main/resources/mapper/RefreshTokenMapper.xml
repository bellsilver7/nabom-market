<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.nabom_market.auth.mapper.RefreshTokenMapper">

    <sql id="columns">
        id, member_id, token_hash, expires_at, revoked_at, created_at
    </sql>

    <select id="findByTokenHash" resultType="RefreshToken">
        SELECT <include refid="columns"/>
        FROM refresh_token
        WHERE token_hash = #{tokenHash}
    </select>

    <insert id="insert">
        INSERT INTO refresh_token (member_id, token_hash, expires_at)
        VALUES (#{memberId}, #{tokenHash}, #{expiresAt})
    </insert>

    <update id="revokeIfActive">
        UPDATE refresh_token
        SET revoked_at = NOW()
        WHERE token_hash = #{tokenHash} AND revoked_at IS NULL
    </update>

    <update id="revokeAllByMemberId">
        UPDATE refresh_token
        SET revoked_at = NOW()
        WHERE member_id = #{memberId} AND revoked_at IS NULL
    </update>

</mapper>
