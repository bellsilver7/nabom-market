<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.nabom_market.order.mapper.OrderMapper">

    <sql id="columns">
        id, member_id, total_price, status, ordered_at, updated_at
    </sql>

    <select id="findById" resultType="Order">
        SELECT <include refid="columns"/>
        FROM orders
        WHERE id = #{id}
    </select>

    <select id="findByIdAndMemberId" resultType="Order">
        SELECT <include refid="columns"/>
        FROM orders
        WHERE id = #{id}
          AND member_id = #{memberId}
    </select>

    <select id="findAllByMemberId" resultType="Order">
        SELECT <include refid="columns"/>
        FROM orders
        WHERE member_id = #{memberId}
        ORDER BY id DESC
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO orders (member_id, total_price, status)
        VALUES (#{memberId}, #{totalPrice}, #{status})
    </insert>

    <update id="update">
        UPDATE orders
           SET status = #{status}, total_price = #{totalPrice}
         WHERE id = #{id}
    </update>

</mapper>
