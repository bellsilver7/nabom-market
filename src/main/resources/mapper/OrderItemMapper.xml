<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.nabom_market.order.mapper.OrderItemMapper">

    <!-- 상품명이 필요해 product 를 조인한다.
         단가는 product.price 가 아니라 order_item.order_price (주문 시점 스냅샷)를 쓴다. -->
    <sql id="viewColumns">
        oi.order_id    AS orderId,
        oi.id          AS id,
        p.id           AS productId,
        p.name         AS productName,
        oi.order_price AS orderPrice,
        oi.quantity    AS quantity
    </sql>

    <select id="findAllByOrderId" resultType="OrderItemView">
        SELECT <include refid="viewColumns"/>
        FROM order_item oi
        JOIN product p ON p.id = oi.product_id
        WHERE oi.order_id = #{orderId}
        ORDER BY oi.id DESC
    </select>

    <select id="findAllByOrderIds" resultType="OrderItemView">
        SELECT <include refid="viewColumns"/>
        FROM order_item oi
        JOIN product p ON p.id = oi.product_id
        WHERE oi.order_id IN
        <foreach collection="orderIds" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        ORDER BY oi.id DESC, oi.order_id ASC
    </select>

    <insert id="bulkInsert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO order_item (order_id, product_id, quantity, order_price)
        VALUES
        <foreach collection="items" item="item" separator=",">
            (#{item.orderId}, #{item.productId}, #{item.quantity}, #{item.orderPrice})
        </foreach>
    </insert>

</mapper>
